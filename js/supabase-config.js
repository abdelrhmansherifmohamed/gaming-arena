// Supabase config stub for frontend. The backend uses server-side Supabase.
// This file exists to avoid 404 when referenced from index.html.
// If you need a frontend Supabase client, set these values and initialize the client here.
window.SUPABASE_CONFIG = window.SUPABASE_CONFIG || {};
